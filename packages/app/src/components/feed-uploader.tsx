export default function FeedUploader() {
  /**
   * - get old last update from state
   * - verify if last update in the state is not older than the remote one
   *    - if yes notify user for confirmation
   * - patch last update in the state.
   * - push to remote
   */
}
